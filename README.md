# Home Lab setup


## Requirements

    - Proxmox Ve Host
    - Docker 
    - Docker compose

## Dashboard

Homer.yml


## Terraform Folder

```bash
 docker compose -f docker-compose.yaml run --rm terraform init
```
```bash
 docker compose -f docker-compose.yaml run --rm terraform init
```
Hint: TF must match VM Template specs